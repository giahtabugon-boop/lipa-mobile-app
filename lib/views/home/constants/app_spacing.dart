import 'app_sizes.dart';

class AppSpacing {
  static double xs = AppSizes.scale(4);
  static double sm = AppSizes.scale(8);
  static double md = AppSizes.scale(16);
  static double lg = AppSizes.scale(24);
  static double xl = AppSizes.scale(32);
}
